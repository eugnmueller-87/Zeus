# ZEUS — Python dependencies
# Install: pip install -r requirements.txt

# Market data
yfinance>=0.2

# Interactive Brokers API (EU-compatible, German residents)
ib_insync>=0.9.86

# Vector DB for pattern learning (Phase 2)
chromadb>=0.4

# HTTP requests (Hermes API + Telegram alerts)
requests>=2.31

# Scheduling (standalone mode fallback)
apscheduler>=3.10

# Environment variable management
python-dotenv>=1.0
