# ZEUS — Python dependencies
# Install: pip install -r requirements.txt

# Claude API — ZEUS LLM reasoning layer
anthropic>=0.40

# Market data
yfinance>=0.2

# Interactive Brokers API (EU-compatible, German residents)
ib_insync>=0.9.86

# Vector DB for pattern learning (Phase 2)
chromadb>=0.4

# HTTP requests (Hermes API + Telegram alerts)
requests>=2.31

# Upstash Redis — shared intelligence layer with SpendLens
upstash-redis>=1.0

# Scheduling (standalone mode fallback)
apscheduler>=3.10

# Environment variable management
python-dotenv>=1.0

# Quality gate — testing
pytest>=8.0
pytest-mock>=3.12
